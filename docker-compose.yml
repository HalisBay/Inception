version: '3.8'

services:
  mariadb:
    container_name: mariadb
    build:
      context: /home/hbay/srcs/requirements/mariadb/.
    env_file:
      - /home/hbay/srcs/.env
    volumes:
      - data:/var/lib/mysql
    # ports:
    #   - "3306:3306"
    networks:
      - wpress
      
  wordpress:
    container_name: wordpress
    build:
      context: /home/hbay/srcs/requirements/wordpress/.
    env_file:
      - /home/hbay/srcs/.env
    volumes:
      - /home/hbay/srcs/wordpress_data:/var/www/html
    # ports:
    #   - "8080:8080"
    depends_on:
      - mariadb
    networks:
      - wpress

  nginx:
    container_name: nginx
    build:
      context: /home/hbay/srcs/requirements/nginx/.
    ports:
      - "443:443"
    volumes:
      - /home/hbay/srcs/wordpress_data:/var/www/html
    depends_on:
      - wordpress
    networks:
      - wpress

volumes:
  data:
    driver: local
  wordpress_data:
    driver: local

networks:
  wpress:
    driver: bridge